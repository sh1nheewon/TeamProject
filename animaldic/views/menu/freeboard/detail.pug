extend ../../layout

block content
    link(rel="stylesheet", href="/css/free3.css")
    script(src="/js/detail.js") 
    h2.free 게시글 보기
      main
        .write
          input(type='text' readonly name="title" required='' value=`${bd.title}`)
          hr
          textarea.text(name="content" readonly)=bd.content
          div.free
            div.user
                input(type="text" name="num" readonly value=`${bd.author}`)
            div.freebutton(data-num=`${bd.num}`)
                button.update(data-num=`${bd.num}`) 수정
                button.delete(data-num=`${bd.num}`) 삭제
                button.list 돌아가기