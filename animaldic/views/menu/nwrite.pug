extends ../layout

block content
    link(rel='stylesheet', href='/css/nwrite.css')
    script(src="/js/insert_notice.js") 
    script(src="/js/nwrite.js") 
    main
        form.write(method="POST",enctype="multipart/form-data")
          input(type='text' name="title" placeholder='제목을 입력해주세요.' required='')
          hr
          textarea.text(name="content" placeholder='내용을 입력해주세요.')
          div.free
            div.freebutton
                input.btn.btn_post(type="button", value= "등록")
                input.btn.btn_list(type="button", value="돌아가기")